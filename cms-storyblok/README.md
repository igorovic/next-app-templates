Should work when you create your first space on storyblok.com

# Important

You need to publish once the page `Home` and also configure it's real path to `/`

# Https proxy

Use `Caddy proxy` as an devlopment https proxy
